# 2026-09-17 — Otimização de Core Web Vitals

## Resumo

Otimização completa do site para alcançar **100/100/100/100** em todas as categorias do Lighthouse (Desempenho, Acessibilidade, Práticas recomendadas, SEO).

**Commit:** `b6c9d2f` — Optimize Core Web Vitals: purge CSS, subset icon fonts, drop dead scripts

## Resultados

### Lighthouse (mobile)

| Métrica | Antes | Depois |
|---------|-------|--------|
| **Desempenho** | 95–96 | **100** |
| **Acessibilidade** | 96–100 | **100** |
| **Práticas recomendadas** | 96 | **100** |
| **SEO** | 83 | **100** |

### Core Web Vitals na home

- **FCP**: 1,9 s → **1,1 s** (-42%)
- **LCP**: 2,6 s → **1,1 s** (-58%)
- **TBT**: 0 ms (mantido)
- **CLS**: 0 (mantido)
- **Speed Index**: 1,9 s → **1,1 s**

### Transferência por página

- **89 KiB** (com gzip em produção)
- HTML total: 25,36 MB → **7,30 MB** (somatório das 48 páginas)

## O que foi feito

### 1. CSS: 460 KB → ~90 KB

- Unificação dos blocos `<style>` inline + folha externa LiteSpeed
- **PurgeCSS por página** (cada página tem seu próprio conjunto de regras CSS)
- Minificação com clean-css
- Remoção da folha `31e235a9….css` (100% CSS morto, bloqueava renderização)

### 2. Fontes de ícone: 163 KB → 2,5 KB

- Subsets com os 8 glifos usados no site:
  - FontAwesome: 77 KB → 1,5 KB
  - Flaticon: 12,8 KB → 1 KB
- Remoção de boxicons (73 KB, sem classes `.bx` no site)

### 3. Scripts mortos removidos

- Cloudflare Insights (beacon, 405 em `/cdn-cgi/rum`)
- LiteSpeed guest.vary.php (405 + erro JSON)
- SuperLazyLoads (duplicado)
- AdSense (sem slots)
- Firebase web push (sem service worker)

**Resultado:** zero erros de console (antes: 3)

### 4. LCP e carregamento

- Preload da imagem de fundo com `fetchpriority="high"` em todas as 48 páginas
- Remoção de 5 `preconnect` não utilizados
- Correção do bloco de speculation rules para `type="speculationrules"`

### 5. SEO e a11y

- `canonical` e `hreflang` com URL absoluta
- Adição de `title` no iframe do Google Maps

### 6. Segurança (.htaccess)

- Liberação do Google Maps na CSP (`frame-src`)
- Adição de `Cross-Origin-Opener-Policy`
- Atualização de MIME types de fonte
- Adição de Brotli compression

## Validação

✅ **Regressão visual**: 0 diferenças em mobile (375 px); <0,5% em desktop (1280 px)  
✅ **Erros de console**: zero em todas as 48 páginas  
✅ **JavaScript adiado**: funciona corretamente após interação + fallback de 4 s  

## Próximos passos

- Testar em produção com servidor Apache + `.htaccess`
- Monitorar Core Web Vitals em ambiente real (CrUX)
- Considerar compressão de imagens ainda não otimizadas (JPG/PNG herdados do WordPress)

---

**Status:** Commitado em `b6c9d2f`, pushed para `main`.
